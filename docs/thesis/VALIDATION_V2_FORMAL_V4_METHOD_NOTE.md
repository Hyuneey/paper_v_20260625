# VALIDATION V2 Formal V4 Method Note

## Selected execution authority

`DEC-020 = APPROVED_FORMAL_V4`; `DG-01 = RESOLVED_BY_USER`.

VALIDATION V2 formally adopts the versioned V4 relational-rule descriptor and
its deterministic validity, numeric binding, replay, portfolio-freeze, and
runtime-authorization controls as the scientific execution authority.

The execution stack is:

1. `CanonicalRuleDescriptorV4`;
2. V4 task validity;
3. V4 replay/conformance controls;
4. VALIDATION V2 Utility V4 numeric authority;
5. V4 portfolio freeze and runtime authorization;
6. durable prediction custody;
7. versioned metric and result-integrity controls.

Canonical `RuleV1` and `VerifierV1` remain adjacent rule-validity components.
They are not claimed to directly authorize the V4 runtime. The
canonical-to-V4 bridge is not selected and is not required for the minimum
thesis path.

## Claim boundary

Formal V4 controls executable eligibility, provenance, replay, numeric binding,
custody, and integrity. It does not prove scientific utility, causal or physical
truth, generalization, operational value, or explanation usefulness.

PILOT V1 remains immutable. This note applies prospectively to VALIDATION V2
only and does not rewrite historical bridge-option analyses.
