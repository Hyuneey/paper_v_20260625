# TASK-039E3 R2R Utility Normal-Only Authority V1 — Protocol Freeze

## Status

`passed_task039e3_r2r_utility_normal_only_authority_v1_implementation_and_freeze`

This freeze implements a new deterministic normal-only numeric-authority boundary for the frozen COMMON-42 executable structure. It does not restore historical E1, materialize real numeric values, authorize utility execution, or alter historical construction science.

## Scientific interpretation

The future experiment, if separately authorized after independent audit, will evaluate the frozen COMMON-42 executable relation/rule structure under a newly versioned deterministic normal-only calibration authority. It will not claim to evaluate byte-identical historical E1 numeric rule instances.

- Authority version: `TASK039E3_UTILITY_NORMAL_ONLY_AUTHORITY_V1`
- Lineage: `NEW_VERSION_FROM_FROZEN_METHOD_AND_NORMAL_DATA`
- Historical E1 identity restored: `false`
- Historical numeric-registry identity restored: `false`
- Historical construction-result validity: `UNCHANGED`
- Historical terminal-custody validity: `UNCHANGED`

## Frozen authority inputs

Route C is bound by:

- Feasibility audit: `eb2406391e189d3c53613e2c0074d4575cc91403fe047e54066b5a418906e415`
- Dependency matrix: `bbeffb23ec9310f9572e1ab6657d6d18b2ec93e62b18687a70567dcec6ccd74d`
- COMMON-42 authority check: `3bd07e1c2baf375bde86a2310b529dda40962e027edbd77485f431dc244730ff`
- Executable equivalence: `3efdce159bc5ac39825d4e4654428237e47205307f83aae7a133db6c5789f60f`

The committed public evidence independently yields exactly 42 relation identities, 42 semantic-execution identities, and 420 unique utility-required numeric-role bindings. T0/T1/T1-B remain the COMMON executable class with 42 accepted and zero `no_rule` cells.

## Normal-input boundary

Only the exact HAI 23.05 P1 normal train1 and train2 files may be used during a future authorized materialization. Their expected byte hashes are:

- train1: `53007b0ba604fbf338e7ac2e08cd81d874b5d1388f3aecb213ddcba5bf2bec4a`
- train2: `0e520e82bf78a661ab19ce4967f3c766bd809820f457a9c90c365102d4534c56`

The identities are cross-bound to the committed HAI dataset manifest and D1 data-access audit. Both files must pass filename, byte-size, and SHA-256 checks before CSV values are parsed. Train3, test files, and labels are outside this authority.

## Calibration semantics

Ten roles are frozen. Three are normal-data derived:

1. `source_step_threshold` via `derive_multi_file_source_parameters_v1`.
2. `source_stability_tolerance` via the same function and its frozen threshold preimage.
3. `target_noise_scale` via `derive_multi_file_target_scale_v1`.

The other seven are exact `PreregisteredWindowConstantBundleV1` fields. All formulas, file-local differencing/windowing, MAD scaling, Q75 interpolation, minimum-amplitude gate, signs, and domains are unchanged. No new quantile, tuning, relation discovery, direction selection, horizon search, or train3 confirmation is permitted.

## New reference and registry contract

Every new reference is value-independent and binds the new authority version, relation binding, semantic-execution hash, role, calibration-policy version, exact normal-input identity set, and COMMON-42 authority. The computed value enters only the final record/content hash.

The future private registry requires exactly 420 records and 420 unique `(relation_binding_hash, numeric_role)` keys. It rejects missing, duplicate, unexpected, malformed, nonfinite, historical-identity, relation, semantic, input, and authority-version mismatches.

No real private registry was created in this task.

## Durable custody and atomic finalization

Future materialization requires both an explicit private destination and the matching `TASK039E3_UTILITY_NORMAL_ONLY_AUTHORITY_V1` environment locator. The private destination and local locator manifest must remain outside Git.

The finalization sequence is: validate frozen code and inputs; compute and validate all records; write/reopen a temporary private registry; atomically rename it; write and validate the local-only locator; reopen and rehash the private registry; then write the sanitized public receipt last. A partial file or registry without the final receipt is not authoritative.

The public receipt excludes numeric values, raw HAI values, absolute private paths, labels, and credentials.

## T2 boundary

`T2_UTILITY_SCOPE_AUTHORIZED = false`. Historical aggregate status remains 39 accepted and three `no_rule`, but the unavailable exact private membership cannot authorize a relation-level T2 utility arm.

## Claim boundary

After later successful materialization and audit, it is permissible to state that a new deterministic normal-only authority was materialized from the frozen COMMON-42 structure and frozen calibration method. It must also be described as method-continuous but identity-new.

It is not permissible to say historical E1 was recovered, the historical numeric registry was reproduced, the new values were proven identical to E1, or exact historical numeric rule instances were utility-tested unless separately proven later.

## Current boundary and next gate

Real normal values, test values, labels, attack intervals, utility values, provider calls, API keys, and scientific LLM calls accessed in this task: zero/false. No real private authority was materialized.

The next task is an independent audit of this authority V1 only. Real materialization remains unauthorized.
