# TASK-039E3 R2R Utility Protocol Remediation

Status: `passed_task039e3_r2r_utility_protocol_remediation`

The original Protocol V1 remains immutable and retains its historical
`FROZEN_BUT_BLOCKED_BY_INDEPENDENT_AUDIT` classification. The canonical
protocol is now the additive `BASE_V1_PLUS_REMEDIATION_V2` overlay in
`src/paperworks/v6/task039e3_r2r_utility_protocol_v2.py`. It is frozen pending
the focused re-audit; this task grants no evaluator, label, utility, Rule v2,
runtime, sealed, provider, or winner authority.

## Authority closure

- The exact successful-run E1 ledger verified at
  `0998c6600078b8a0aca7263b6e0b702808cc141b1cbcfe3d0026fddb98c408a7`.
- The private registry is outside Git and binds 420/420 required references;
  missing, ambiguous, nonfinite, label/test-derived, and Direct-number records
  are all zero. Its public hash is
  `59e81b261801f28eefc917256dc628af704a14b4064161972d01545968555271`.
- Logical test1 and test2 coordinates now map uniquely to physical file-local
  rows. The 120 logical purge cells map to no feature or label row and enter no
  event, exposure, metric, or interpreter denominator.
- The utility source universe is the exact frozen 12-source P1 set bound by
  source identity hash
  `0af3f80f18a3eab59b9783af64d306c8d774eeb69b3a72c24c10048abd4ed234`.

## State, metric, and interpretation closure

Source indices lacking complete source context do not form opportunities.
Only retained, isolated, matching-direction events form the abstention
denominator. Target-side file/split/incomplete/nonfinite states abstain with no
alarm and deterministic reason precedence. `no_rule` produces no interpreter,
opportunity, alarm, or abstention and remains diagnostic-only at relation
level; system recall is built from the union/deduplicated portfolio alarms.

Strict binary labels accept only integer objects 0 and 1. A future private
label/event custody object must derive events and normal exposure from the same
physical label vector. Normal FAR/hour therefore has no free exposure input.
Event F1 preserves the complete precision and recall component preimages and
has no fabricated count pair.

T2 reporting uses the exact signs of recall and FAR deltas with no materiality
margin or weighted score. `COMMON-42` is only a prediction identity and has no
single construction cost. T2 call deltas are +42 versus T0, 0 versus T1, and
-84 versus T1-B.

## Verification and authority

Seventy focused, V1 regression, and historical blocked-audit preservation
tests passed. Compileall, pip dependency consistency, Git diff checks, source
blob/raw-byte verification, JSON self-hashes, deterministic bundle binding,
and task-scoped privacy scans passed. Label values, HAI test features, utility
values, providers, credentials, and scientific execution were not accessed.

Remediation bundle:
`5880ebff1fbda2004dfa3d955376b88d21972721b5287bc5fd62b9e71a6df2cb`

Exact next task:
`TASK-039E3-R2R-UTILITY-PROTOCOL-REMEDIATION-FOCUSED-RE-AUDIT`
