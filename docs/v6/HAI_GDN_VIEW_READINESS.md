# HAI GDN View Readiness

TASK-039B creates a one-second candidate-learning view for the selected process
but does not fit normalization parameters. Future normalization may be fit only
on `normal_candidate_fit`.

No authoritative GDN view is created. The production graph-ranking backend is
unresolved, downsampling is not approved, model training is unauthorized, and
a CandidateUniverse mask remains mandatory. TASK-039C must choose a validated
GDN-aligned backend or use a different scientifically accurate backend name.
