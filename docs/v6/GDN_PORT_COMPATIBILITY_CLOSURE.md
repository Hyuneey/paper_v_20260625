# GDN Port Compatibility Closure

TASK-039C-GDNP classifies the explicit sparse-softmax and `node_dim=0`
bindings as `documented_non_scientific_api_adapter` changes. They alter only
dependency API binding and preserve the upstream equations and execution
policy.

The compatibility closure requires:

- all twenty API matrix rows resolved;
- exact official and installed source identities;
- the prior fidelity, softmax, environment, wheelhouse, and GDNC failure
  receipts retained and hash-bound;
- independent GraphLayer forward/backward and optimizer parity;
- explicit source/destination, self-loop, batch-boundary, and aggregation
  evidence;
- full GNNLayer, tiny full-GDN, and tiny training-loop gates;
- an empty unresolved-compatibility list;
- no HAI values, META, STAT, or BR2 pair outcomes used to design the closure.

Rootless Podman is unavailable on the approved host, so the optional legacy
PyG 1.5 synthetic oracle is recorded as
`blocked_official_legacy_environment_unavailable`. This is nonblocking because
the immutable-source audit and all independent modern-port gates are required
to pass.

The closure does not authorize any causality, confirmed-relation, rule,
anomaly-performance, or method-superiority claim.
