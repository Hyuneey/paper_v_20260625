# TASK-039E3 Rule-Construction Scientific Execution

TASK-039E3 is the first authorized construction-only execution of the frozen
E0/E1/E2 protocol. It uses the exact OpenAI model snapshot and stateless strict
structured output. One non-scientific synthetic capability probe must pass
before any scientific call.

The execution processes all 42 relations with deterministic T0, one-call T1,
exactly three independent T1-B calls, bounded deterministic-controller T2,
and the one-call direct-number ablation. Provider, proposal-validity,
construction-outcome, and direct-number custody remain outside Git. Only
sanitized aggregate results and hashes may be committed.

This task cannot access HAI, labels, attacks, detector utility, or candidate
method outcomes. It cannot select a winner, materialize Rule v2, or grant
runtime authority. A blocked capability gate or low construction validity is
a valid fail-closed scientific outcome; no model or configuration fallback is
allowed.
