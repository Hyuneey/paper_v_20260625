# TASK-039E3-R2R-UTILITY-OUTER-D0-D1-D2V1-EXECUTION-V1

## Frozen scope

Perform exactly one local, sealed HAI-23.05 test2 execution of the already
authorized D0 detector-only, D1 COMMON-42 Rule-only, and exact-same-second D2
V1 combined arms.  The shared feature file is opened, hashed, and semantically
parsed once.  All three label-blind predictions are atomically persisted,
closed, reopened, self-hash checked, and row-closure checked before the label
file is opened, hashed, and parsed once.

The execution accepts no scientific caller parameters.  D0 uses the exact
frozen PCA-SPE model and threshold; D1 uses the exact frozen COMMON-42
authority, numeric registries, source-census policy, and evaluator semantics;
D2 V1 requires at least two distinct alarming sources at the exact same
decision second and preserves every D0 alarm.  D2 V2, temporal tolerance,
score gating, refit, recalibration, rule changes, parameter search, retry, and
post-OUTER redesign are prohibited.

## Ordering and custody

The implementation and synthetic tests are committed before any test2 access;
independent tests are committed separately without changing production.  A
single path-redacted private-custody sentinel must pass before the sole test2
feature access.  Detailed D1 relation evidence, D2 fusion evidence, and metric
evidence remain outside Git.  Public prediction artifacts use a frozen compact
index-set representation with exact 230400-row round-trip closure.

## Scientific accounting

One OUTER attempt, zero retries, one D0 inference, one complete D1 rule
evaluation, one D2 V1 fusion, one label parse after prediction freeze, six
primary metric computations, seven secondary metric computations, zero test1
feature access, zero D2 V2 execution, and zero result-driven changes.  Result
magnitude is not a pass criterion.  Execution freezes numbers only; scientific
interpretation remains unavailable until an independent result-integrity
audit passes.

## Stop boundary

No push and no remote egress.  On success stop with the frozen OUTER result and
authorize no further execution.  The exact next task is
`TASK-039E3-R2R-UTILITY-OUTER-D0-D1-D2V1-RESULT-INTEGRITY-AUDIT-V1`.
