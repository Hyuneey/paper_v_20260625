# TASK-039D2R — Result-Contract Recovery

TASK-039D2R repairs only the frozen D2 execution-receipt schema's deterministic
one-key/four-key mismatch. The original scientific execution code remains the
exact Commit A `5524262d8a666093f948f7f01491b4a0b03e568e`.

The recovery finalizer has no HAI loader or dataset-root input. It validates the
original 45-record private confirmation ledger, reconstructs public directional
and pair outcomes, freezes those artifacts, and only then loads public candidate
provenance for descriptive arm metrics. It does not open train3 or any other HAI
file and does not recompute events, responses, or confirmations.

The scientific result remains one-way train3 calibration confirmation. Rule v2,
LLM/Agent construction, detector/runtime authority, and method-winner selection
remain prohibited pending an independent TASK-039D2 audit.
