# TASK-039E3-R2R D2 V2 R5 execution-accounting field remediation R1

## Authority and scope

This local-only task may inspect the frozen public D2 V2 execution-accounting
artifact, its frozen producer, the committed R5 harness, blocker, report, and
continuity metadata. It must not reopen any scientific prediction, source-map,
horizon-map, CombinedPredictionV2, private evidence, label, feature, test2, or
OUTER authority. It creates no seventh integrity-audit attempt and performs no
scientific execution or oracle replay.

The known accounting correction is exact and schema-driven:

- noncanonical R5 expectation: `d1_metric_reads`;
- frozen canonical field: `d1_metric_artifact_reads`;
- required canonical value: `0`.

Fuzzy aliases, frozen-artifact edits, retries, result-driven changes, private
leakage, and push are prohibited.

## Completion gate

Completion is permitted only if the committed R5 blocker evidence binds the
complete required scientific-oracle snapshot and the committed R5 control flow
proves that scientific, private-evidence, leakage, and report-schema checks all
completed before the accounting lookup. If either condition is false, freeze a
sanitized blocker without reopening scientific data.

## Expected stop boundary

The frozen R5 harness is authoritative over remembered or chat-derived state.
No task may infer omitted snapshot facts, rerun an oracle, or promote R5 to a
completed audit when its committed order or blocker evidence is insufficient.

