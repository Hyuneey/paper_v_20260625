# TASK-039E2-PREP: Provider-Neutral Construction Execution Freeze

## Status

`passed_task039e2_execution_freeze_preparation`

This status means only that provider-neutral contracts, deterministic
renderers, closed schema drafts, and synthetic tests are prepared. It does not
select a provider or model, authorize E2, contact a provider, generate a real
proposal, create Rule v2, or grant runtime authority.

## Base and boundary

- Base: `20ca2e6f561ce0cdfaf822198f7b64d8e143215c`.
- Branch: `task-039e2-execution-freeze-prep`.
- Real E1 result and private construction evidence are unavailable to this
  branch.
- All provider, model, relation, request, evidence, and numeric fixtures are
  clearly synthetic and all numeric values are fake.
- No provider SDK or network client is imported.

## Future configuration and capability freeze

`ConstructionExecutionConfigurationV1` carries the future provider and exact
model identifiers, capability-receipt hash, prompt version/hash, structured
schema hash, decoding controls, output limit, seed policy, stateless-call
requirement, timeout, retry-policy hash, E0 budget hash, schedule hash, and
construction-input rendering-policy hash. PREP accepts only `SYNTHETIC_*`
identifiers and keeps provider/model selection, provider contact, and execution
authority false.

`ModelCapabilityReceiptV1` records structured-schema output, temperature,
deterministic or exposed-seed control, stable model version, stateless calls,
and input/output token sufficiency. Missing requirements must be disclosed
exactly and the execution-capability check then fails closed; nothing is
silently emulated.

## Private construction view

`ConstructionInputViewV1` renders one synthetic confirmed relation with exact
directions/horizon, eleven approved numeric roles, fake approved values,
numeric/evidence references, seven window constants, and bounded semantic
metadata. `ConstructionEvidenceRenderingPolicyV1` freezes the role set,
eight-field metadata bound, private calibrated-value input allowance, and
reference-only main output requirement.

Raw HAI, labels, attacks, test outcomes, utility results, and candidate-method
results are rejected recursively and by explicit false boundary flags.

## Prompt and retrieval fairness

- T1, every T1-B call, and T2 call 1 render the exact same immutable
  scientific content and initial-corpus hash.
- T1-B calls 1-3 are stateless, feedback-free, and expose no prior T1-B
  proposal or verifier result.
- T2 follow-up input is limited to bounded verifier issue codes, affected
  fields, an optional same-corpus targeted representation, and the previous
  proposal hash. Chain-of-thought is prohibited.
- `T2RetrievalCorpusPolicyV1` permits only targeted re-presentation of evidence
  identities already present in the initial view. It introduces no new
  scientific information and permits one retrieval action.
- T1-DIRECT-NUMBER retains the T1 relation/semantic structure, but PREP binds
  no scientific numeric roles and cannot render a real call. A later E2 freeze
  must explicitly bind those roles first.

## Structured output and T0

The main structured output remains the closed E0 candidate family
`canonical_delayed_response_rule_v1_candidate`. Unknown fields, prose/runtime
code, unsupported variables, arbitrary numeric literals, prohibited data
references, mismatched evidence references, provider-selected controller
actions, and authority preclaims fail closed.

Synthetic T0 uses the existing deterministic E0 template and produces the same
closed DSL family, relation/direction/horizon binding, approved numeric
references, and window references. It is a synthetic parity test only; no real
T0 proposal is generated.

## Schedule, retries, and custody

`ConstructionExecutionScheduleV1` requires all 42 relations in frozen E0
cohort order, deterministic arm ordering, one shared configuration hash,
result-independent ordering, and the exact maximum scientific budget:

- T1: 42 calls;
- T1-B: 126 calls;
- T2: at most 126 calls;
- T1-DIRECT-NUMBER: 42 calls;
- maximum total: 336 calls; and
- T0 provider calls: 0.

The PREP schedule is explicitly synthetic and not the future actual schedule.
Scientific generation retry is `none`. Transport retry is possible only for a
predeclared transport failure before any model response. Malformed, invalid,
low-quality, and verifier-rejected responses consume a scientific call.

Provider custody receipts record request identifier when available, model,
call number, response presence, structured parse result, transport retry
count, and proposal hash. The prepared retention policy is sanitized
structured-output-first; raw model output and chain-of-thought are not stored.

## Authority receipt

- Real E1 result consumed: `false`.
- Real E1 private evidence accessed: `false`.
- Real confirmed identities consumed: `false`.
- Provider selected: `false`.
- Model selected: `false`.
- Provider contacted: `false`.
- LLM called: `false`.
- Real T0 generated: `false`.
- Rule v2 authorized: `false`.
- Runtime authority: `false`.
- E2 authorization created: `false`.

No global task status or downstream authority is changed.
