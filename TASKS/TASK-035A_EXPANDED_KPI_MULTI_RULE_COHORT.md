# TASK-035A: Expanded KPI Multi-Rule Generation Cohort

Status: implementation prepared; real execution requires the clean Commit A
gate and the bounded DEC-055 approval.

TASK-035A defines E2X-G as 10 eligible KPI series, five deterministic
generation anchors per KPI, and two identical one-shot provider requests per
anchor. It includes sealed-prefix loading, prompt fidelity, receipt-first
capture, static auditing, rootless-container runtime-contract checks, and an
aggregate adequacy report.

The task prohibits test parsing, KPI performance evaluation, inner/outer rule
selection, RepairAgent, ReviewAgent, retries, detector/fusion execution, host
generated-rule execution, and tracked raw prompts, responses, rules, arrays, or
outputs.

Completion requires two commits: Commit A freezes implementation and policy
before provider access; Commit B records aggregate execution results only.
