# Runtime Authorization Audit

General canonical `runtime_authority.py` is a separate synthetic RuleV1 lane and did not govern frozen D1. Frozen D1 used an exact V4 authority, evaluator bundle, private numeric resolver custody, committed execution grant and one-attempt token. These task-specific checks reject stale descriptors, registry references, census or scope changes.
