# D2 result lineage

## V1

Frozen D0 bytes + frozen D1 bytes + source map → committed V1 authorization →
exact-index two-source fusion → private fusion-evidence persistence → public
`ScientificCombinedPredictionArtifactV1` durable persistence → label custody
open → contiguous event/episode metrics → frozen comparison and integrity
reports. Every edge is **VERIFIED** from source and frozen public metadata.

## V2

The same upstream predictions + source map + frozen native-horizon map →
separate V2 authorization → active-token construction → horizon-based two-
source fusion → durable `ScientificCombinedPredictionArtifactV2` → label open →
metrics → completed integrity evidence and scientific disposition. Every edge
is **VERIFIED**. The scientific role is **TEST1_INFORMED_DEVELOPMENT**.

No frozen D0-versus-D2 significance test exists. Metrics were not recomputed in
ARCH-009.
