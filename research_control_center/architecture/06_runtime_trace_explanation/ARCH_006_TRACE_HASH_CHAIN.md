# Frozen D1 trace and prediction hash chain

```text
COMMON-42 descriptor hash
+ ten numeric reference identities
+ frame hash
+ isolated source-event identity
+ opportunity ID
+ committed authorization / bridge identity
→ computation_identity

computation_identity
+ final state
+ alarm and decision index
+ opportunity / event / relation identities
+ numeric reference identities
→ task-specific trace_hash

ordered prediction records
+ authority / source / census bindings
+ counts and label-blind declarations
→ ScientificRulePredictionArtifactV1 artifact_hash
```

The frozen result-integrity audit independently replayed the prediction artifact, its 6,031 unique opportunities, 6,031 trace hashes, authority bindings, and zero mutation result.

This chain supports task-specific auditability. It does not turn the records into canonical `RuntimeTraceV1`: there are no nine satisfaction steps, canonical rule/verifier IDs, parameter values and units, or independently persisted trace documents.

Object/schema/authority classification: `NON_EQUIVALENT`. Only the terminal outcome concepts are `PARTIALLY_OVERLAPPING`. Detection outcome binding is strong within the V4 plane; canonical trace/explanation interoperability is not established.
