# MULTIPANEL-PRE-DG05-FREEZE-001 report

Status: `COMPLETE_QA_PASS_PRE_DG05_FROZEN`

The HAI23 detector method was replayed without refit. Its existing private model bytes and separate PCA/IF fit and threshold receipts are now bound by an exact, path-free public hash authority; no model or score bytes were deserialized during this closure. HAI22 and HAI21 PCA-SPE and secondary Isolation Forest were re-instantiated using only label-blind normal projections under the unchanged HAI23 algorithms. The external detector feature universes are prospectively verified canonical intersections (24 and 22 fields), so both are `SCHEMA_BOUND_PARTIAL_DETECTOR_REPLICATION`; they are not new detector methods.

All three future panels have exactly M0 PCA, M1 T0, M2 T2, M3 PCA+T0, and M4 PCA+T2 as primary methods. Fusion retains same-file, same-physical-second, FAIL-only, at-least-two-distinct-source, detector-preserving OR semantics. HAI23 alone retains V2A as a secondary evidence-rich reference; no external V2A was created.

Primary evaluation is file-local official-scenario overlap for independently classified `P1_ELIGIBLE` scenarios, reported version by version with Wilson 95% intervals. The V2 metric authority binds every scenario hit to exact dataset, physical file, official scenario, scenario-authority, and eligibility-authority identities; paired tables require those identities to match. eTaPR is secondary and is bound to a canonical file-namespaced disjoint union while retaining canonical per-file outputs. Official-source synthetic tests passed file-order, separator-size, cross-file-boundary, independent-oracle, per-file, and empty-input checks while preserving the prior 109/109 receipt.

The hardened V2 global custody contract binds the exact public physical-file census, panel-specific detector-complete positive feature allowlists (HAI23 37, HAI22 24, HAI21 22), Phase-A raw/header/source hashes, and the complete panel × physical file × exact method-component census. Each method cell is derived from frozen detector, T0/T2/V2A, and Fusion roots rather than caller-supplied hashes. Each cell terminates with a distinct success or method-failure receipt. Success receipts are structurally replayed against durable prediction bytes; projections are replayed for exact column order, row count, and hash; failure receipts contain no invented prediction/alarm fields. A no-overwrite append-only state-receipt chain requires state-specific durable artifacts, and the durably replayed global manifest must reach `GLOBAL_PREDICTION_FROZEN_LABEL_LOCKED` before the opaque single-issue/single-consume lease can open. Lease issuance advances a durable `LABEL_SCENARIO_LEASE_OPEN` transition; `RESULTS_COMPUTED` requires a separate durable result-integrity receipt. Frozen projection and prediction bytes are replayed both before and after the label reader, while reader failure still consumes the lease. `METHOD_FAILURE` is never coerced to no alarm, no rule, or miss. The P1 V2 custodian rejects arbitrary self-consistent mappings and binds exact per-version identities and source authorities plus official source/scenario hashes. Label and scenario non-contact attestations cover parse, decode, inspect, count, validate, filter, and use. No real attack payload, label, scenario authority, eligibility record, or prediction was opened or generated in this task.

Backup status remains `SINGLE_COPY_LOCAL_ONLY`. The professor package remains `NOT_SUBMITTED`.

Current public handoff indexes are `PANEL_REGISTRY_V5.csv` and `IMPLEMENTATION_TASK_INDEX_V5.csv`; prior versions remain historical and unchanged.
