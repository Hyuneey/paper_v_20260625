# Hidden train3 reference 평가

모든 arm output이 durable freeze된 후 기존 V2A normal-confirmed directional relation authority를 replay한다.
고정 union의 pair별 집합으로 평가하고 multiple valid directions를 보존한다.
명칭은 FROZEN_NORMAL_CONFIRMED_RELATION_REFERENCE.
causal/physical/independent held-out ground truth가 아니다.
구조 reference는 이전 normal protocol의 개발 근거이며 독립 검증이라는 주장을 하지 않는다.
semantic exact match는 numeric alias/evidence ID를 제외한다.
source/target direction, horizon, directional set P/R/F1와 pair decision을 각각 보고한다.
그 후 train4 guard를 한 번 적용한다. 어떤 hidden result도 proposal을 재생성하지 않는다.

