# 두 단계 실행 동결

Commit A: 구현·synthetic tests·성능 점검·private 보존·문서. 이 commit은 과학 실행 결과가 아니다.

A가 clean/pushed일 때 run_gdn_front_exp04.py freeze가 정확한 source/config/schema/preregistration/portfolio/selected numeric policy/sidecar/environment/official metadata identity를 생성한다. Commit B는 A를 직접 부모로 하고 이 freeze JSON 한 파일만 추가해야 한다. B가 origin과 일치하고 clean일 때만 execute를 허용한다.

정상 데이터로 기존 PCA/IF를 fit/calibrate한 후 원래 frozen policy 및 새 실행 identity를 결속한다. 원래 V2A authority source commit은 바꾸지 않는다. 모델은 atomic 저장 및 exact bytes/receipts/tree-state/normal calibration score 재생을 거친다. sklearn pickle의 재직렬화 byte layout은 불변하지 않을 수 있어 실행에는 원래 live hash-bound model을 유지한다.

test1 feature 접근 직전에 durable marker를 남긴다. 이후 scientific source/config 변경이 필요하면 BLOCKED_POST_FEATURE_EXECUTION_REVISION_REQUIRED이며 자동 수정·재실행하지 않는다. 모든 5개 dense predictions와 native D1이 동결되고 coordinate census가 일치한 뒤 opaque one-shot label capability를 발급한다. Labels는 capability callback 안에서만 읽고 semantic parse는 한 번 수행한다.

Commit C는 결과/QA/report/dashboard만 포함한다. 결과가 음성이어도 frozen artifacts를 변경하지 않는다.
