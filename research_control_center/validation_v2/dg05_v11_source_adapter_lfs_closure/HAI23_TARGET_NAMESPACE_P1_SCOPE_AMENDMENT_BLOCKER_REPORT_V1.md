# HAI23 direct-target namespace / P1 closure — blocked

## Verdict

`P1_SCOPE_REQUIRES_PROSPECTIVE_SOURCE_AMENDMENT`

The frozen P1 contract is an exact finite canonical-identity membership
contract, not a representation-independent process-membership contract. It
therefore cannot classify official HAI23 boiler-DCS direct targets that are not
members of its closed HAI23 canonical feature set.

## Evidence

The closed HAI23 scenario authority independently replays at 38 records. The
official technical manual identifies the boiler as P1, distinguishes HAIEnd
internal DCS points from HAI data points, permits attack through internal
algorithm blocks, and places the HAI23 attack operation in the boiler-control
DCS. A private worksheet identifies 14 non-canonical target representations,
24 occurrence-level tokens, and 21 affected scenarios; public evidence keeps
only the set hash and aggregate counts.

The frozen scope contains 86 HAI23 canonical identities, 44 P1 identities,
and returns `UNRESOLVED` for a non-member. Its already frozen multi-target
semantics is `ANY` verified-P1 target; otherwise unresolved direct targets
prevent a deterministic out-of-scope decision.

## No mutation or performance contact

No alias, namespace authority, P1 eligibility authority, scope expansion,
denominator, prediction, metric, method, Rule, threshold, Fusion, or release
was changed. This report contains no raw target values, intervals, labels,
feature values, predictions, private paths, or credentials.

See the attached decision brief for the only permissible next authority.
